import actions from './types'

const initialState = {

};

export default (state = initialState, action) => {
    const payload = action.payload
    switch (action.type) {

        default:
            return state;
    }
};
